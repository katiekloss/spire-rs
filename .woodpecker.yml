when:
  - event: push
  - event: manual

labels:
  backend: docker

steps:
  - name: test
    image: rust:1-alpine
    commands:
      - cargo +nightly test

  - name: demo
    image: rust:1-alpine
    commands:
      - cargo +nightly run --release --bin=demo